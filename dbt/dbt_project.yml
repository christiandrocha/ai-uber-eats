name: ai_uber_eats
version: "1.0.0"
config-version: 2

profile: ai_uber_eats

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ai_uber_eats:
    gold:
      +materialized: incremental
      +catalog:      aiubereats
      +schema:       payments
